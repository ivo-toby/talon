## Database Queries

Use `db_query` to run read-only SQL queries against the Talon database.
This is useful for inspecting threads, messages, runs, schedules, and other
operational data. Queries are read-only — mutations are not allowed.
