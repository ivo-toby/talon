[Unit]
# Installed only when `install-service.sh --codex-runner` is selected.
# Starting talond then starts the contained runner first on every boot.
Requires=talon-codex-runner.service
After=talon-codex-runner.service
